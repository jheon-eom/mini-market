server:
  port: 8083

spring:
  application:
    name: payment-service

  # Distributed Tracing with Sleuth & Zipkin
  sleuth:
    sampler:
      probability: 1.0  # 모든 요청 추적 (프로덕션에서는 0.1 등으로 조정)
    baggage:
      remote-fields:
        - traceId
        - spanId
      correlation-fields:
        - traceId
        - spanId
  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
    service:
      name: ${spring.application.name}

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: payment-service-group
      auto-offset-reset: earliest
      enable-auto-commit: false
    producer:
      acks: all
      retries: 3

  datasource:
    url: jdbc:mysql://localhost:3309/minimarket_payment_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: payment_user
    password: payment_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: false
    open-in-view: false

#  data:
#    redis:
#      host: localhost
#      port: 6382
#      password:
#      timeout: 3000ms
#      lettuce:
#        pool:
#          max-active: 8
#          max-idle: 8
#          min-idle: 0
#          max-wait: -1ms

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

jwt:
  secret: 1fed56241e80f07310227d1529dd928087620bf836eed6802d1843197db83501
  access-token-validity: 3600000  # 1시간
  refresh-token-validity: 604800000  # 7일